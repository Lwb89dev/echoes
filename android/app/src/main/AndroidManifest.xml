<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Relay sync (websockets) and attachment upload/download (HTTP) both
         need this. Only the debug/profile manifests had it before (added
         there by the Flutter template for hot-reload tooling only), which
         meant a release build could never reach a relay at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Voice note recording. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!--
        allowBackup=false: without it, Android Auto Backup (Google Drive)
        and `adb backup` would copy this app's private data off the device
        — including the Hive notes box (plaintext whenever the optional
        at-rest password encryption is off, which is the default) and
        SharedPreferences. For an app holding a Nostr identity and private
        notes/attachments, that off-device copy is exactly what must not
        happen; the private key's own keystore-encrypted blob would be
        useless without the (non-backed-up) keystore key anyway, but the
        notes themselves would not be. fullBackupContent/dataExtractionRules
        are redundant once backup is disabled outright.
    -->
    <application
        android:label="Echoes"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
